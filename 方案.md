# 善愿日用品店 · 产品约定（v1）

这是第一版的产品说明，方便以后改功能时对照。不含任何账号、密码、密钥。

## 定位

厂家 / 店铺商品册 + 意向购物车 + 联系商家成交。不是淘宝式商城，网站里不收款、不下单。

## 角色

- **游客**：看店、看分类、看详情、看客服。不能加购物车。
- **登录用户**：游客能力 + 购物车 + 改用户名 / 密码 / 头像。注册不绑邮箱、不绑手机。
- **管理员**：独立账号（账号 + 密码 + 密钥）。改店铺、客服、分类、商品；可重置客户密码（用户名不变）；可在用户视角直接摆货。

## 价格

一个商品可以有多条报价，每条是「金额 + 数量 + 单位」，前台显示如 `100元5个`。

## 上传

图片在浏览器里压成 jpg 再传到 R2。视频建议 mp4 / webm，单条不超过 80MB。苹果 HEIC 若浏览器读不了，请先另存为 jpg / png。

## 部署

整站跑在 Cloudflare：Worker + D1 + R2。本仓库不存放管理员口令；本地用 `.dev.vars`，线上用 `wrangler secret put`。
